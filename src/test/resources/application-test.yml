spring:
  flyway:
    out-of-order: true
    validate-on-migrate: false
    baseline-on-migrate: false

rate-limit:
  enabled: false

logging:
  level:
    root: WARN
    ao.angogas: INFO
    org.flywaydb: INFO
